@model Imengur.Models.ImageAndUsersHome;
@{
    ViewData["Title"] = "Home Page";
}

<div class="text-center">
    <h1 class="display-4">Recent images:</h1>

    @foreach (var image in @Model.Images)
    {

@*        <div class="card" style="width: 18rem;">
            <img class="card-img-top" src="~/uploads/@image.ImageData" alt="Card image cap">
            <div class="card-body">
                <h5 class="card-title">@image.Title</h5>
                <p class="card-text">@image.Description</p>
                <a asp-controller="Image" asp-action="SearchImage" asp-route-Id="@image.Id" class="btn btn-primary">Go to photo</a>
            </div>
        </div>*@

        <a asp-controller="Image" asp-action="SearchImage" asp-route-Id="@image.Id">
            <img class="img-thumbnail w-25" src="~/uploads/@image.ImageData" />
        </a>
    }
</div>
